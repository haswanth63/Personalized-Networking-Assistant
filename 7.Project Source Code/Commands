# 1. Navigate to project
cd "D:\Intership Projects\Personalized-Networking-Assistant"

# 2. Activate virtual environment
venv\Scripts\activate

# 3. Install all packages
python -m pip install fastapi uvicorn[standard] streamlit pydantic transformers accelerate requests httpx python-dotenv wikipedia-api pandas numpy python-multipart loguru pytest pytest-cov pytest-asyncio

# 4. Install PyTorch
python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# 5. Verify
python -c "import fastapi, streamlit, transformers, torch; print('✅ All packages installed!')"

# 6. Start backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 7. Start Frontend Server
streamlit run frontend/streamlit_app.py